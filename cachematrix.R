makeCacheMatrix <- function(x = matrix()) {

i <- NULL
y <- function(n) {
z <<- n
i <<- NULL
}


## I changed the "sInverse" and "putInverse"

cacheSolve <- function(x, ...) {
        ## Return a matrix that is the inverse of 'x'
put <- function() z
yInverse <- function(inverse) i <<- inverse
placeInverse <- function() i
list(set = set,
get = get,
yInverse = sInverse,
placeInverse = placeInverse)
}
